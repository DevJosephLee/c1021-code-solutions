/* exported isUpperCased */
// create storage for uppercased word, and assign uppercased word.
// give back the comparison between uppercased word and original word.
function isUpperCased(word) {
  var upperCasedWord = word.toUpperCase();
  return upperCasedWord === word;
}
