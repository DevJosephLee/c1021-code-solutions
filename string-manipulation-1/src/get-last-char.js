/* exported getLastChar */
// create a storage for last character, and assign the last character
// give back last character of string
function getLastChar(string) {
  var lastCharacter = string.length - 1;
  return string[lastCharacter];
}
