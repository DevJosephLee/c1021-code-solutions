/* exported isLowerCased */
// create storage for lowercased word, and assign lowercased word.
// return the comparison between lowercased word and original word.
function isLowerCased(word) {
  var lowerCasedWord = word.toLowerCase();
  return lowerCasedWord === word;
}
