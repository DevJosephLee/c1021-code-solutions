/* exported zip */
function zip(first, second) {
  // var firstCopy = [];
  // var secondCopy = [];
  // firstCopy.push(first);
  // secondCopy.push(second);
  // for (var i = 0; i < first.length; i++) {
  //   for (var j = 0; j < second.length; j++) {
  //     if (firstCopy[i].length !== secondCopy[j].length) {
  //       firstCopy[i].pop();
  //       secondCopy[j].pop();
  //       console.log('first', firstCopy[i]);
  //       console.log('second', secondCopy[j]);
  //     }
  //   }
  // }
}
